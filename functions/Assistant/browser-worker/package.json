{
    "name": "alldone-browser-worker",
    "version": "1.0.0",
    "private": true,
    "description": "Isolated Playwright browser worker for the assistant's browser_* tools. Deployed as its own Cloud Run service; never bundled into Cloud Functions.",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "echo 'Run from the repo root: npx jest --config ci/jest.functions.config.js functions/Assistant/browser-worker'"
    },
    "engines": {
        "node": ">=22"
    },
    "dependencies": {
        "express": "4.21.2",
        "playwright": "1.49.1"
    }
}
