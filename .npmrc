; Stage 5 (Node 22 / npm 10): the RN-era dependency tree predates npm 7's
; peer-dependency enforcement and cannot satisfy it (react-native 0.61-era
; packages peer-pin ancient react versions). npm 6 ignored peers; keep that
; resolution behavior explicit so npm ci works identically locally and in CI.
legacy-peer-deps=true
